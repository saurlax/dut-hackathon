# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "dut-hackathon"

[setup]
script = '''
set -e

: "${CODEX_WORKTREE_PATH:?CODEX_WORKTREE_PATH 未设置}"

GIT_COMMON_DIR="$(git rev-parse --path-format=absolute --git-common-dir)"
PROJECT_ROOT="$(dirname "$GIT_COMMON_DIR")"
SOURCE_ENV_PATH="$PROJECT_ROOT/.env.local"

if [ ! -f "$SOURCE_ENV_PATH" ]; then
  echo "主工作树不存在 .env.local：$SOURCE_ENV_PATH" >&2
  exit 1
fi

cp -p "$SOURCE_ENV_PATH" "$CODEX_WORKTREE_PATH/.env.local"
cd "$CODEX_WORKTREE_PATH"

npm ci

npm run db:migrate
npm run db:seed'''
