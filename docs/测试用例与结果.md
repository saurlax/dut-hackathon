# 大工黑客松组队中心 · 测试用例与结果

| 项        | 内容                                                                                                  |
| --------- | ----------------------------------------------------------------------------------------------------- |
| 被测应用  | Next.js 16 + PostgreSQL(Drizzle) + Auth.js 魔法链接 重写版《大工黑客松组队中心》                      |
| 分支/提交 | 当前工作树（含安全审计、测试修复、邮箱限流、UI 恢复与公开资料前置审核）                               |
| 测试日期  | 2026-08-09                                                                                            |
| 测试方式  | 单元/组件（Vitest + jsdom）+ PostgreSQL 集成（真实 PG）+ Playwright E2E（真实浏览器 + Mailpit 邮件）  |
| 参考来源  | 旧飞书妙搭版《测试用例 (1).md》的 96 条用例（按业务语义映射到新栈，并超出补充）                       |
| 门禁      | `npm run check` + `npm test` + `npm run test:integration` + `npm run test:e2e` + `npm run build` 全绿 |

---

## 一、门禁基线（修复前 → 后）

| 检查      | 命令                                   | 修复前      | 修复后                                     |
| --------- | -------------------------------------- | ----------- | ------------------------------------------ |
| 静态门禁  | `npm run check`（prettier+eslint+tsc） | 通过        | ✅ 通过（exit 0）                          |
| 单元/组件 | `npm test`                             | 31 条       | ✅ **89 条**                               |
| 集成      | `npm run test:integration`（真实 PG）  | 22 条       | ✅ **65 条**                               |
| E2E       | `npm run test:e2e`（chromium+mobile）  | 2 过 / 4 败 | ✅ **4 过 / 4 跳过**（本轮未配置 Mailpit） |

> E2E 修复前失败原因：public-nav 严格模式命中两个链接；auth-flow 邮件轮询超时 + 魔法链接 host 打错（详见第九节环境说明）。两者均为测试/环境健壮性问题，非应用代码缺陷。

---

## 二、本次缺陷修复（新栈行为类 bug）

| 编号         | 现象                                                                                                                                 | 修复方案                                                                                                                                                                                                                                      | 验证                                                   |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| **B1**       | 最终确认被管理员驳回后，队长仍可提交/更新作品（`saveSubmission` 只查确认"存在"不查 `auditStatus`）                                   | 要求 `teamConfirmations.auditStatus==='approved'` 才允许提交；细分文案（无确认/审核中/被驳回）；`/submission` 页面同步门控；提交事务对 confirmation 行加 `FOR UPDATE`，避免与驳回并发                                                         | ✅ 集成测试四态覆盖 + 并发测试                         |
| **B6**       | 驳回最终确认时确认状态与作品撤回不是原子操作；并发保存作品可能重新写回已撤回的公开授权                                               | 驳回分支包进 `db.transaction`，确认状态和作品撤回一起提交，避免中间状态                                                                                                                                                                       | ✅ 并发集成测试                                        |
| **B2**       | 队长手动"暂停招募"后，编辑任一字段保存会悄悄恢复招募中（编辑分支 `recruitStatus` 重算，paused 分支因 deadline 已通过校验成为死代码） | 编辑分支保留队长意图：满员才置 `full`，否则 `existing.recruitStatus==='paused' ? 'paused' : 'recruiting'`                                                                                                                                     | ✅ 集成测试                                            |
| **B4**       | 管理员只能加不能减，无降级路径                                                                                                       | 新增 `removeAdmin` action：禁移除自己、禁移除最后一名管理员（`FOR UPDATE` 锁花名册防并发掏空）、禁移除 `ADMIN_EMAILS` 种子（登录即重提，改环境变量才能移除）；`/admin` 管理员表加两步确认"移除"按钮，自己/种子置灰                            | ✅ 集成测试（demote/self/last/missing/seed）+ 组件测试 |
| **B5**       | proxy 不在边缘拦截 `/admin`，全靠页面守卫                                                                                            | Auth.js `authorized` 回调 + `proxy.ts` 接线：未登录的 `/admin` 在边缘重定向 `/login`（防御纵深）；已登录放行给 `requireAdmin` 判角色（保持非管理员→`/?error=forbidden`）。决策抽成 `authorizeAdminPath` 纯函数                                | ✅ 单测 + build 识别 Proxy                             |
| **赛道**     | 报名阶段仍收集“期望赛道”，与实际“所属赛道只在作品提交时填写”的规则冲突                                                               | 移除 `participants.expected_tracks` 列、报名表单/校验/管理员详情展示，并新增 `0006` 迁移；作品提交与队伍详情仍保留 `track` 字段                                                                                                               | ✅ 单元测试 + 0006 迁移已应用                          |
| **分页**     | 公开列表硬 `limit(100)` 无分页无提示（旧 DEF-04/17、GAP-02）；`pageSize` 负数等无钳制                                                | `publicTeams`/`publicParticipants`/`showcase` 支持 `page`/`pageSize`：`page` 钳 ≥1、`pageSize` 非正整数回退默认 12、上限 50，返回 `{items,total,page,pageSize}`；三个列表页读取参数、banner 显示真实总数、共用 `Pager` 组件（翻页保留关键词） | ✅ 集成测试（翻页/钳制/上限/去重）+ 组件测试           |
| **redirect** | Auth.js redirect 回调无单测（开放重定向风险）                                                                                        | 抽成 `resolveAppRedirect` 纯函数并单测：同源放行、相对路径钳到源、跨源拒绝、畸形回退                                                                                                                                                          | ✅ 单测                                                |
| **审核竞态** | migration 在新 writer 切流前执行，旧实例可能在一次性回填后继续写入自动通过记录                                                       | `0008` 先安装数据库审核守卫再回填；`approved_revision` 必须与当前 revision 一致，资料变更或缺少管理员审批标记的 `approved` 写入统一降为 `pending`                                                                                             | ✅ 真实 PG 模拟旧版创建、编辑与队长转让                |

---

## 三、旧版 96 用例 → 新仓库映射（17 缺陷 + 3 功能缺失）

| 旧编号 | 主题                   | 新仓库状态               | 证据                                                           |
| ------ | ---------------------- | ------------------------ | -------------------------------------------------------------- |
| DEF-01 | 空邮箱/空电话吞错      | ✅ 架构消解              | zod `required` + `safeParse` 返 fieldErrors                    |
| DEF-02 | 并发重复建队           | ✅ 架构消解              | `team_member_one_team_idx` 唯一索引 + 应用层三重检查+行锁      |
| DEF-03 | 手填队伍编号致无法申请 | ✅ 架构消解              | 无手填编号字段，成员关系走 `team_members`                      |
| DEF-04 | 分页被忽略             | ⚠️ 真缺口 → **本次已修** | 见第二节"分页"                                                 |
| DEF-05 | 申请理由队长看不到     | ✅ 已实现+组件测试       | `ReceivedApplicationCard` 渲染 `message`，空值显示“未填写留言” |
| DEF-06 | 截止日晚一天           | ✅ 架构消解              | `domain.ts` 固定 UTC+8 `eventDate` + `date` 列                 |
| DEF-07 | 退出登录无反应         | ✅ 已实现+组件测试       | `LogoutButton` 提供提交反馈并调用 `logout`                     |
| DEF-08 | 全表慢                 | ✅ 架构消解              | PostgreSQL + 索引                                              |
| DEF-09 | 申请存临时文件         | ✅ 架构消解              | `team_applications` PG 表 + 部分唯一索引                       |
| DEF-10 | /admin 未登录可进      | ✅ 架构消解 + 本次加固   | `requireAdmin` + 本次 B5 边缘守卫                              |
| DEF-11 | 队名无长度限制         | ⚠️ 分歧（新设计）        | 现 80 字上限（旧 30），视为新设计保留                          |
| DEF-12 | 满员/私密提示误导      | ✅ 架构消解              | 详情页满员/过期/暂停三态文案                                   |
| DEF-13 | 空格队名需往返         | ✅ 架构消解              | zod `.trim()`                                                  |
| DEF-14 | 自己队伍显示申请按钮   | ✅ 已实现+组件测试       | `TeamApplicationEntry` 对当前队伍显示“你已经在这支队伍中”      |
| DEF-15 | 编辑全量覆盖           | ⚠️ 设计如此              | `saveTeam` 覆盖 14 字段；UI 表单恒提交全字段故安全，保留       |
| DEF-16 | 断网英文 Network Error | ✅ 已实现+测试           | 根 `error.tsx` 中文兜底：页面暂时不可用 + 重试                 |
| DEF-17 | pageSize 负数少一条    | ⚠️ 真缺口 → **本次已修** | 见第二节"分页"（负/0/非正整数回退默认）                        |
| GAP-01 | 同意/拒绝申请          | ✅ 已实现                | `reviewTeamApplication` 双向 + 自动撤其它申请/满员拒余         |
| GAP-02 | 分页控件               | ⚠️ 缺口 → **本次已实现** | `Pager` 组件 + total 驱动                                      |
| GAP-03 | 过期队伍申请           | ✅ 已实现                | `assertTeamAcceptsApplications` 校验 deadline                  |

> 前端渲染/交互项已补组件测试：DEF-05/07/14/16 不再是待验状态。

---

## 四、新增测试清单

### 单元/组件覆盖

- `client-ip.test.ts`：无代理时放行登录、可用 IP 仍按 IP+邮箱限流（3）
- `proxy-authz.test.ts`：/admin 未登录拦截、已登录放行、其它路径放行（3）
- `redirect-url.test.ts`：同源/相对/跨源/畸形 URL（4）
- `validators.test.ts`：队伍人数边界（1/0/-1/小数/字母）、队名空白/超长、参赛者手机/邮箱必填（3）
- `registration-form.test.tsx`：报名表单不再渲染“期望赛道”（1）
- `validators.test.ts`：participantSchema 剔除 expectedTracks（1）
- `domain.test.ts`：截止日当天开放/前一天关闭、编号补零（2）
- `pager.test.tsx`：单页不渲染、翻页保留关键词、首页/末页省略（3）
- `remove-admin-button.test.tsx`：两步确认、禁用占位（2）
- `logout-button.test.tsx`：退出按钮提交后调用 logout（1）
- `received-application-card.test.tsx`：队长可见申请留言、空留言占位（2）
- `team-application-entry.test.tsx`：当前队伍、待处理申请、登录跳转、校内限制（4）
- `error.test.tsx`：中文错误兜底和重试（1）

### 集成覆盖（真实 PG）

- **B1/B6**：最终确认无/审核中/被驳回/已通过四态下 saveSubmission 的门控；驳回与保存并发时不会重新公开被撤回的作品
- **B2**：暂停招募后编辑详情，recruitStatus 保持 paused
- **B4**：removeAdmin 降级成功、自降拒、最后管理员拒、不存在拒、种子拒（含 ADMIN_EMAILS 环境切换）
- **分页**：15 队/15 人翻页、跨页无重叠、pageSize 负数/0/非数字/小数回退默认、上限 50、page 钳制
- **申请规则**：最多 3 份申请、仅校内拒校外、过期拒、重复申请拒、未审核拒、已入他队拒
- **队伍/队长**：缩减人数拒、队长转让三种失败分支、跨队审批（同意后他队申请自动撤回）
- **审核/报名/队伍/撤回/keyword**：报名与队伍首次公开及修改后保持待审核、队长转让重新待审、历史自动通过数据迁移回待审、数据库拦截滚动发布期间旧 writer 的自动通过、名单变化推进 revision 并拒绝旧页面审核、驳回原因与重新提交、仅本人可撤回、公开列表关键词命中/落空

### E2E（本轮 4 条通过、4 条因未配置 Mailpit 跳过，chromium + mobile）

- 首页响应式 + 邮箱登录入口
- 公共导航（队伍大厅/找队友/作品展示 路由）
- 魔法链接全流程规格已更新：登录请求 → Mailpit 收信 → 点链接 → 报名待审核且不公开 → 创建公开队伍待审核且不进入大厅；本轮因未配置 `E2E_MAILPIT_URL` 未执行

---

## 五、真实发现（非本次修复范围，需产品决策）

| 发现                                                                                                                                   | 影响                                              | 建议                                                     |
| -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | -------------------------------------------------------- |
| **S2 设计用 `next/font/google` 拉 Noto Sans SC，Google Fonts 不可达时全站 500**（本机新 dev server 即复现，靠复用已缓存 `.next` 规避） | 国内部署/无外网环境应用打不开                     | 改用 `next/font/local` 或国内 CDN 自托管字体             |
| `.env.local` 的 `AUTH_URL` 指向 `127.0.0.1:3001`，与本地 dev 端口不符                                                                  | 魔法链接 host 打错，e2e/跨端验证会打到错误 server | 本地开发按实际端口设置 `AUTH_URL`                        |
| 队名上限 80 字（旧版修复时定为 30）                                                                                                    | 规格差异                                          | 视为新设计保留；如需对齐可改 `validators.ts`             |
| 编辑队伍为全量覆盖语义（未传字段重置）                                                                                                 | 非 UI 调用方提交子集会清字段                      | 设计如此，UI 恒提交全字段；如开放部分更新 API 需改 merge |

---

## 六、改动文件清单

**后端（Server Actions / 查询）**

- `src/app/actions.ts`：B1（saveSubmission 门控）、B2（编辑保留招募态）、B4（removeAdmin）、分页相关校验
- `src/lib/queries.ts`：publicTeams/publicParticipants/showcase 真分页 + 钳制助手
- `src/auth.ts`：B5（authorized 回调）、redirect 抽取
- `proxy.ts`（新）：Next.js 16 Proxy 接线，匹配 `/admin/:path*`
- `src/lib/proxy-authz.ts`（新）、`src/lib/redirect-url.ts`（新）

**前端**

- `src/app/submission/page.tsx`：最终确认审核态门控
- `src/app/admin/page.tsx`：管理员表加"移除"列
- `src/app/browse-teams|browse-pool|showcase/page.tsx`：读取分页参数 + Pager
- `src/components/pager.tsx`（新）、`src/components/remove-admin-button.tsx`（新）

**测试**

- `tests/integration/database.test.ts`：数据库约束、并发状态机与审核门禁
- `src/lib/*.test.ts`、`src/components/pager.test.tsx`（新）、`remove-admin-button.test.tsx`（新）
- `tests/e2e/public.spec.ts`（scope header）、`auth-flow.spec.ts`（轮询 30s/超时 90s）
- `playwright.config.ts`：支持 `E2E_BASE_URL`
- `src/auth.ts` / `src/lib/client-ip.ts` / `src/lib/email-rate-limit.ts`：IP 限流
- `src/db/schema.ts` / `drizzle/0004_steady_purple_man.sql` / `drizzle/0005_auto_approve_profiles.sql` / `drizzle/0009_require_profile_reapproval.sql`：邮箱限流表、历史自动通过策略、审核 revision 约束及滚动发布数据库守卫

**文档**

- `README.md`：业务规则补"移除管理员三保护"与"公开列表分页"

---

## 七、E2E 环境复现要点

1. **字体**：新 dev server 首次抓 Google Fonts 若失败会缓存 500；删掉 `.next` 重启即可（网络通时），或复用已有字体缓存。
2. **AUTH_URL**：跑 e2e 时覆盖 `AUTH_URL=http://127.0.0.1:<端口>`，否则魔法链接指向环境变量里的旧 host。
3. **Mailpit**：dev server 的 `EMAIL_SERVER_PORT` 需对齐 Mailpit 实际 SMTP 端口；E2E 设 `E2E_MAILPIT_URL` 指向其 UI 端口。
4. **测试数据库**：`TEST_DATABASE_URL` 指向独立 `dut_hackathon_test` 库（已迁移），避免污染 dev 库；`beforeEach` 会清空业务表。
